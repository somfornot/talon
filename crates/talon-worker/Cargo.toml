[package]
name = "talon-worker"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "Worker node: stores and serves cached object data for Talon"
# The crate ships the `talon-loadgen` benchmark driver alongside the server, so
# a bare `cargo run -p talon-worker` is otherwise ambiguous. Name the server so
# the documented command works without `--bin`.
default-run = "talon-worker"

[[bin]]
name = "talon-worker"
path = "src/main.rs"

[[bench]]
name = "observability_benches"
harness = false

[[bench]]
name = "serve_path_benches"
harness = false

# Concurrent load generator for the data plane (#291). A manual tool for
# comparing runtimes at realistic connection counts; not a CI gate.
[[bin]]
name = "talon-loadgen"
path = "src/bin/loadgen.rs"

# Serve-path probe (#540): syscall counts, RSS, and concurrency for the paged
# zero-copy path. Manual tool for a known machine, not a CI gate.
[[bin]]
name = "talon-serve-probe"
path = "src/bin/serve_probe.rs"

# Benchmark-only Talon frame proxy. It is deliberately separate from the
# production worker so latency and fault injection cannot leak into serve code.
[[bin]]
name = "talon-client-bench-proxy"
path = "src/bin/client_bench_proxy.rs"

[dependencies]
talon-core.workspace = true
talon-metadata.workspace = true
prost.workspace = true
talon-transport.workspace = true
talon-backend = { path = "../talon-backend" }
anyhow.workspace = true
tokio.workspace = true
tracing.workspace = true
tracing-subscriber.workspace = true
bytes.workspace = true
async-trait.workspace = true
futures.workspace = true
clap.workspace = true
xxhash-rust.workspace = true
libc.workspace = true
monoio.workspace = true
serde.workspace = true
serde_json.workspace = true
axum.workspace = true
tempfile.workspace = true

[dev-dependencies]
divan.workspace = true
tokio = { workspace = true, features = ["test-util"] }

[[bench]]
name = "dataplane_benches"
harness = false

[[bench]]
name = "rate_limit_benches"
harness = false

[build-dependencies]
prost-build.workspace = true
